import React from 'react';

export class BlackKey extends React.Component {
    render() {
        let component = (
            <div className="black-key"></div>
        );
        return component;
    }
}